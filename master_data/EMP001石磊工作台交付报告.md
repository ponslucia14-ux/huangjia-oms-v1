# EMP001 石磊工作台交付报告

生成时间：2026-07-10

阶段：Frontend Delivery Phase

范围：EMP001 石磊老板工作台

## 1. 交付目标

本轮只交付前端工作台，不新增后端能力，不新增 AI，不新增 Engine，不新增 Domain。

目标页面：

- 首页工作台
- 销售中心
- 财务中心
- 运营中心
- 数据追溯

数据原则：

- 使用生产 API
- 使用 OMS_TRUTH_SOURCE
- 使用真实数据
- 不使用 Mock
- 不使用 legacy_runtime

## 2. 当前静态前端状态

服务器 current release 静态文件已同步到：

```text
/opt/huangjia-oms/current/oms_app/
```

已同步文件：

- app.js
- contract.json
- styles.css
- index.html
- oms-config.js

服务器文件校验：

```text
app.js        f1bdf39ab68a3bcfa4c7899ad81c5b1b535a5325e8ee104a1716ec297786864f
contract.json c5c253fe6ece4bc0f535932a3d375084f34292b1826558757356bcadde73198a
index.html    ee907ea93bc90b3bbad7234e1864f782631635ef7b90086794cd75fdba84d9f0
oms-config.js 32eb2cc013148ad447bdd2763b3dc46d456490986a33f4a39f39d640811df5a8
```

## 3. 数据来源

生产 API：

```text
http://127.0.0.1:8787
```

生产服务：

```text
huangjia-oms.service
status = active
source = OMS_TRUTH_SOURCE
```

首页接口：

```text
/api/oms/home?user_id=a2c82cb4
status = ready
payload_policy = home_summary_only
payload_size = 9016 bytes
```

首页摘要计数：

```text
resident_data = 8
room_status_data = 42
sales_contract_data = 224
finance_data = 11
financial_events = 1278
stay_records = 172
service_data = 0
```

照护师结构化生产数据状态：

```text
暂无结构化生产数据
```

## 4. 页面与接口绑定

### 4.1 首页工作台

接口：

```text
/api/oms/home
```

数据内容：

- 当前用户与工作台
- 今日经营摘要
- 销售、财务、运营汇总指标
- 风险与待办数量
- 各中心入口
- 数据更新时间

状态：

```text
已完成
```

### 4.2 销售中心

接口：

```text
/api/oms/sales?page=1&page_size=500
/api/oms/contracts?page=1&page_size=500
```

验证结果：

```text
sales total = 224, returned = 224
contracts total = 224, returned = 224
```

状态：

```text
已完成前端绑定
```

### 4.3 财务中心

接口：

```text
/api/oms/finance?page=1&page_size=500
```

验证结果：

```text
finance total = 1278, returned = 500
```

状态：

```text
已完成前端绑定
```

### 4.4 运营中心

接口：

```text
/api/oms/rooms?page=1&page_size=500
/api/oms/stays?page=1&page_size=500
```

验证结果：

```text
rooms total = 42, returned = 42
stays total = 172, returned = 172
```

状态：

```text
已完成前端绑定
```

### 4.5 数据追溯

接口：

```text
/api/oms/sales?page=1&page_size=20&include_trace=1
/api/oms/contracts?page=1&page_size=20&include_trace=1
/api/oms/finance?page=1&page_size=20&include_trace=1
/api/oms/rooms?page=1&page_size=20&include_trace=1
/api/oms/stays?page=1&page_size=20&include_trace=1
```

状态：

```text
已完成前端绑定
```

## 5. 功能清单

已完成：

- 首页工作台读取生产 Home Summary API
- 销售中心点击后读取销售分页接口
- 合同数据点击后读取合同分页接口
- 财务中心点击后读取财务分页接口
- 运营中心点击后读取房态与入住分页接口
- 数据追溯页按需读取 include_trace=1 数据
- 前端不再依赖 Home 返回全部明细
- 前端不再从 legacy_runtime 读取页面数据
- contract.json 已对齐轻量 Home 摘要契约

未完成：

- 飞书客户端石磊实机截图未完成
- 飞书客户端销售中心实际点击验收未完成
- 飞书客户端财务中心实际点击验收未完成
- 飞书客户端运营中心实际点击验收未完成
- 飞书客户端数据追溯实际点击验收未完成

## 6. 页面截图

当前未写入截图。

原因：

```text
需要石磊在飞书客户端完成实机操作后补入。
Codex 当前浏览器工具无法继续访问本地 127.0.0.1 页面，安全策略已阻断。
```

待补截图：

- 首页工作台
- 销售中心
- 财务中心
- 运营中心
- 数据追溯

## 7. 阻塞点

当前唯一阻塞点：

```text
飞书客户端实机验收尚未完成。
```

特别说明：

如果飞书企业入口仍指向 GitHub Pages，则 GitHub Pages 静态资源必须发布到最新前端版本后，石磊才能在飞书客户端看到本轮变更。

当前已同步的是服务器 current release：

```text
/opt/huangjia-oms/current/oms_app/
```

## 8. 当前结论

```text
前端绑定状态 = READY
生产 API 状态 = READY
Truth Source 状态 = READY
飞书客户端实机验收 = PENDING
最终交付状态 = WAITING_FOR_OWNER_FEISHU_VALIDATION
```
